/**
 * Domain model entities representing the core business concepts.
 * 
 * Key entities:
 * - Goal: Represents a workflow goal with action nodes and relationships
 * - ActionNode: Individual action within a workflow
 * - ActionStatus: Enumeration of possible action states
 * - Relationship: Dependencies between action nodes
 * - ActiveTask: Tracks active workflow tasks
 * - ChannelMapping: Maps channels to workflows
 * - Request/Response DTOs: Data transfer objects for API communication
 */
package com.example.mattermost.domain.model; 